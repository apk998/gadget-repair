/*

package com.solvd.gadgetrepair.devices;

@SuppressWarnings("unused")
public enum PhoneParts implements ICost {
        SCREEN(150.00),
        CAMERA_LENS(100.00),
        BATTERY(50.00),
        CHARGING_PORT(50.00),
        LABOR_ONLY(0.00);

        private final double cost;

        PhoneParts(double cost) {
            this.cost = cost;
        }

        @Override
        public double getCost() {
            return cost;
        }
    }

 */